<%#--
by zbd
11.07.2019
++#%>

<% html_title l(:label_administration), l(:label_edit) %>
<% local_assigns[:additional_breadcrumb] = "#{l(:label_edit)}: #{@plan_uploader_setting.column_name}" %>
<%#= breadcrumb_toolbar @plan_uploader_settings %>

<%= toolbar title: t(:label_plan_uploader_settings_edit) %>

<%= labelled_tabular_form_for @plan_uploader_setting do |f| %>
  <%= render partial: 'settings', locals: { f: f, columns: @columns,
                                            colname: @plan_uploader_setting.column_name,
                                            table: @table,
                                            setting_type: @setting_type,
                                            setting_types: @settings_types
  } %>
  <%= styled_button_tag l(:button_save), class: '-highlight -with-icon icon-checkmark' %>
<% end %>
