<% local_assigns[:additional_breadcrumb] = "#{t(:label_edit)}: #{t("activerecord.attributes.head_performance_indicator_value.head_performance_indicator_short")} №#{@value.id}" %>


<% "#{l(:label_edit)} #{HeadPerformanceIndicatorValue.model_name.human}" %>


<%= labelled_tabular_form_for @value do |f| %>

  <%= render partial: 'head_performance_indicator_values/form', locals: {f: f} %>
  <%= styled_button_tag l(:button_save), class: '-highlight -with-icon icon-checkmark' %>

<% end %>
