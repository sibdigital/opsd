<form action="<%= form_url %>"
      method="post"
      id="<%= row.busyness_css_id %>-form"
      class="hol <%= row.toggle_item_class_name %>"
      style="display:none"
      accept-charset="UTF-8"
>
  <input name="utf8" type="hidden" value="✓">
  <input type="hidden" name="_method" value="put">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <%= number_field_tag 'member[busyness]',
                       busyness,
                       min: 0,
                       max: 100 %>
  <p>
    <%= submit_tag t(:button_change), class: "button -highlight -small" %>
    <%= link_to t(:button_cancel),
                '#',
                data: { 'toggle-target': ".#{row.toggle_item_class_name}" },
                class: 'button -small toggle-membership-button' %>
  </p>
</form>
